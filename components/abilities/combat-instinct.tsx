import { TypographyP } from "@/components/ui/typography";
import { distortedStyle } from "@/lib/styles";

export default function CombatInstict() {
  return (
    <TypographyP>
      You cannot be surprise attacked by anything which is possible to perceive
      with ordinary senses. Of course, if you had{" "}
      <span style={distortedStyle}>red actedre dac tedredaced</span> it would be
      even more effective.
    </TypographyP>
  );
}
