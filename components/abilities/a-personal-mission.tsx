import { TypographyP } from '@/components/ui/typography';
export default function APersonalMission() {
  return (
    <div>
      <TypographyP>
        Gain an additional xp trigger:{' '}
        <i>
          Did we resolve a clear injustice we encountered while on the mission?
        </i>
      </TypographyP>
    </div>
  );
}
