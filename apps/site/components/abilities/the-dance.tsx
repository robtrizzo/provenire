import { TypographyP } from "@/components/ui/typography";

export default function TheDance() {
  return (
    <TypographyP>
      For as long as you choose to evade attacks instead of attacking a foe, you
      cannot be caught by someone without special training (
      <i>machinist, flywheel, pipedancer</i>) or a special ability (
      <i>bestial transformation</i>).
    </TypographyP>
  );
}
