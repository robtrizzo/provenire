export type Archetype = {
  name: string;
  questions: string[];
};

export type Background = {
  name: string;
};

export type Heritage = {
  name: string;
};

export type Troublemaker = {
  name: string;
};
