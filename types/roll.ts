export enum RollTypes {
  ACTION = 'action',
  FORTUNE = 'fortune',
  RESIST = 'resist',
  PROJECT = 'project',
}

export enum RollResult {
  FAILURE = 'failure',
  PARTIAL = 'partial',
  SUCCESS = 'success',
  CRIT = 'crit',
}

export enum RollEffect {
  REDUCED = 'reduced',
  NORMAL = 'normal',
}
