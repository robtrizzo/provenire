{
  "name": "provenire",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*"
  ]
}
